---
phase: 01-neural-514-basefunctions-logging
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/basefunctions/http/http_client.py
  - src/basefunctions/io/filefunctions.py
  - src/basefunctions/io/serializer.py
  - src/basefunctions/kpi/registry.py
  - src/basefunctions/kpi/exporters.py
autonomous: true
---

<objective>
## Goal
Logging audit for http, io, kpi subpackages: add warning/error coverage at raises and
unhandled except blocks; remove all logger.debug calls; fix duplicate import in filefunctions.py.

## Purpose
Continue the logging pattern established in plans 01-01 and 01-02 across three more subpackages.
Consistent log levels (warning before raises, error in except blocks) give operators actionable
signals without debug noise.

## Output
5 modified source files. No new files. No behavior changes. All 2356 existing tests pass.
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/STATE.md

## Prior Work
@.paul/phases/01-neural-514-basefunctions-logging/01-02-SUMMARY.md

## Source Files
@src/basefunctions/http/http_client.py
@src/basefunctions/http/http_client_handler.py
@src/basefunctions/io/filefunctions.py
@src/basefunctions/io/serializer.py
@src/basefunctions/kpi/registry.py
@src/basefunctions/kpi/exporters.py
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /python:python_code_skill | required | Before any file modification | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /python:python_code_skill loaded

</skills>

<acceptance_criteria>

## AC-1: No Debug Logs in http, io, kpi
```gherkin
Given the http, io, kpi subpackages
When searching for logger.debug / _logger.debug calls
Then 0 results are found
```

## AC-2: Warnings Before All Raises
```gherkin
Given any raise statement in http_client.py, filefunctions.py, serializer.py, registry.py, exporters.py
When the raise is reached at runtime
Then a logger.warning() or logger.error() call immediately precedes it
```

## AC-3: Errors in Except Blocks
```gherkin
Given except blocks in serializer.py and exporters.py that re-raise or swallow exceptions
When an exception is caught
Then logger.error(..., exc_info=True) is called before re-raise (or logger.warning for silent swallows)
```

## AC-4: All Existing Tests Pass
```gherkin
Given the 2356 passing tests
When running pytest after all changes
Then 2356 passed, 0 failed, 0 skipped
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: http subpackage logging audit</name>
  <files>src/basefunctions/http/http_client.py</files>
  <action>
    Audit http_client.py:
    - Change `get_logger(__name__)` to `logger = get_logger(__name__)` so the logger variable
      is available for calling methods.
    - Add `logger.warning(...)` immediately before the `raise RuntimeError("No response received
      for event")` at line 87.
    - Add `logger.warning(...)` immediately before the `raise RuntimeError(error_msg)` at line 97.
    - Bump file header version to v1.4.

    http_client_handler.py: No changes needed — its 2 except blocks convert exceptions to
    EventResult (graceful handling pattern, not failure paths). It has no debug calls.
    No debug calls exist in either file.
  </action>
  <verify>
    grep -n "logger\." src/basefunctions/http/http_client.py
    # Must show logger = get_logger(__name__), 2 logger.warning calls, no debug calls
    grep -n "logger.debug" src/basefunctions/http/http_client.py src/basefunctions/http/http_client_handler.py
    # Must return 0 results
  </verify>
  <done>AC-1 and AC-2 satisfied for http subpackage</done>
</task>

<task type="auto">
  <name>Task 2: io subpackage logging audit</name>
  <files>src/basefunctions/io/filefunctions.py, src/basefunctions/io/serializer.py</files>
  <action>
    Audit filefunctions.py:
    - Fix duplicate import: change `from basefunctions.utils.logging import get_logger, get_logger`
      to `from basefunctions.utils.logging import get_logger`.
    - Change `get_logger(__name__)` to `logger = get_logger(__name__)`.
    - Add `logger.warning(...)` immediately before each of these raises:
        - line 79: `raise ValueError(f"Unknown file_type: {file_type}")`
        - line 330: `raise RuntimeError(f"Directory '{directory_name}' not found.")`
        - line 356: `raise FileNotFoundError(f"{dir_name} doesn't exist, can't rename file")`
        - line 358: `raise FileExistsError(f"{target} already exists and overwrite flag set False")`
        - line 360: `raise FileNotFoundError(f"{src} doesn't exist")`
        - line 419: `raise RuntimeError("can't delete the root directory ('/')")`
    - Keep the existing `get_logger(__name__).info(...)` calls as-is OR update them to
      `logger.info(...)` for consistency — whichever is less churn.
    - Bump file header version by patch.

    Audit serializer.py:
    - Change `get_logger(__name__)` to `logger = get_logger(__name__)`.
    - Add `logger.error("...", exc_info=True)` before every `except Exception as e:` block
      that re-raises as SerializationError. These are at lines ~163, ~204, ~215, ~224, ~235,
      ~244, ~260, ~269, ~285, ~294, ~344.
    - Add `logger.warning(...)` before `raise ImportError("PyYAML is required ...")` at ~254.
    - Add `logger.warning(...)` before `raise ImportError("msgpack is required ...")` at ~279.
    - Add `logger.warning(...)` before `raise TypeError("serializer_class must be subclass ...")` at ~359.
    - Add `logger.warning(...)` before `raise UnsupportedFormatError(...)` at ~340, ~473, ~505.
    - No debug calls exist in either file — nothing to remove.
    - Bump file header version by patch.

    output_redirector.py: No changes needed — no raises, no except blocks, no debug calls.
  </action>
  <verify>
    grep -n "logger.debug" src/basefunctions/io/filefunctions.py src/basefunctions/io/serializer.py
    # Must return 0 results
    grep -n "logger\." src/basefunctions/io/filefunctions.py | wc -l
    # Must be greater than 6 (at least the 6 new warning calls + logger assignment)
    grep -c "logger\." src/basefunctions/io/serializer.py
    # Must be significantly > 0
    grep "get_logger, get_logger" src/basefunctions/io/filefunctions.py
    # Must return 0 results (duplicate import fixed)
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied for io subpackage</done>
</task>

<task type="auto">
  <name>Task 3: kpi subpackage logging audit</name>
  <files>src/basefunctions/kpi/registry.py, src/basefunctions/kpi/exporters.py</files>
  <action>
    Audit registry.py:
    - Add `import logging` to IMPORTS section.
    - Add `logger = logging.getLogger(__name__)` in the LOGGING section (after CONSTANTS,
      before MODULE-LEVEL REGISTRY).
    - Add `logger.warning(...)` immediately before `raise ValueError(f"KPI provider '{name}'
      bereits registriert")` at line 50.
    - Bump file header version to v1.1.

    Audit exporters.py:
    - Add `import logging` to IMPORTS section (standard library).
    - Add `logger = logging.getLogger(__name__)` after the CONSTANTS section.
    - Add `logger.warning(...)` before `raise ImportError("History ist leer ...")` at ~236.
    - Add `logger.warning(...)` before `raise ImportError(...)` at ~231 (PyYAML/pandas ImportError).
    - Add `logger.warning(...)` before `raise ValueError(...)` at ~293.
    - For the 2 silent except blocks at ~630 and ~970 (`except (ValueError, TypeError): pass`):
      add `logger.warning(...)` (not error — these are data-formatting fallbacks, not failures).
    - Bump file header version by patch.

    collector.py, utils.py, protocol.py: No changes needed — no raises, no except blocks,
    no debug calls.
  </action>
  <verify>
    grep -n "logger\." src/basefunctions/kpi/registry.py src/basefunctions/kpi/exporters.py
    # Must show logger assignments and warning/error calls in both files
    grep -n "logger.debug" src/basefunctions/kpi/registry.py src/basefunctions/kpi/exporters.py
    # Must return 0 results
  </verify>
  <done>AC-1, AC-2, AC-3 satisfied for kpi subpackage</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- src/basefunctions/http/http_client_handler.py (graceful EventResult pattern, no changes needed)
- src/basefunctions/io/output_redirector.py (no raises/except blocks, no debug calls)
- src/basefunctions/kpi/collector.py (no raises/except blocks)
- src/basefunctions/kpi/utils.py (no raises/except blocks)
- src/basefunctions/kpi/protocol.py (protocol definitions only)
- tests/ (no test changes — only logging calls added, no behavior changes)
- pyproject.toml (version managed by automated deployment)

## SCOPE LIMITS
- Do not add debug logs
- Do not change logger.critical calls (none exist in these files)
- Do not refactor existing logic — only add/fix logging calls
- Do not change exception types or messages
- Do not add new dependencies

</boundaries>

<verification>
Before declaring plan complete:
- [ ] grep -rn "logger.debug" src/basefunctions/http/ src/basefunctions/io/ src/basefunctions/kpi/ → 0 results
- [ ] grep "get_logger, get_logger" src/basefunctions/io/filefunctions.py → 0 results (duplicate fixed)
- [ ] logger variable assigned (not just get_logger(__name__) discarded) in http_client.py, filefunctions.py, serializer.py
- [ ] import logging + logger = logging.getLogger(__name__) present in registry.py and exporters.py
- [ ] .venv/bin/python -m pytest tests/ --tb=no -q → 2356 passed, 0 failed, 0 skipped
</verification>

<success_criteria>
- All 5 files modified with correct logging coverage
- 5 files confirmed no-op and left untouched
- Zero debug log calls in http, io, kpi subpackages
- Zero raises without preceding warning/error in modified files
- Zero uncovered except blocks that silently swallow exceptions
- 2356 tests pass unchanged
</success_criteria>

<output>
After completion, create `.paul/phases/01-neural-514-basefunctions-logging/01-03-SUMMARY.md`
</output>
