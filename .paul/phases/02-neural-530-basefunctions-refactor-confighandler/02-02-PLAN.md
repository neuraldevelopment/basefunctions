---
phase: 02-neural-530-basefunctions-refactor-confighandler
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ~/.claude/_docs/python/basefunctions/config.md
  - docs/basefunctions/config.md
autonomous: true
---

<objective>
## Goal
System- und User-Dokumentation für das `config` Subpackage vollständig neu schreiben — passend zur neuen App-controlled config loading Architektur.

## Purpose
Aktuelle Docs beschreiben die alte API (YAML, .get(), .has() laut Issue). Nach dem Refactor muss die Dokumentation das neue Konzept korrekt abbilden: `register_package_defaults`, `load_config_file`, `get_config_parameter`.

## Output
- `~/.claude/_docs/python/basefunctions/config.md` — System Doc (technisch, intern)
- `docs/basefunctions/config.md` — User Doc (wie benutzt man das Subpackage)
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Prior Work
@.paul/phases/02-neural-530-basefunctions-refactor-confighandler/02-01-SUMMARY.md

## Source Files
@src/basefunctions/config/config_handler.py
@src/basefunctions/config/__init__.py

## Templates
@~/.claude/_support/python/system_documentation.md
@~/.claude/_support/python/user_documentation.md
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /python:python_code_skill | optional | Nicht nötig (keine Code-Erstellung) | — |

No blocking skills for documentation work.
</skills>

<acceptance_criteria>

## AC-1: System Doc vollständig und korrekt
```gherkin
Given ~/.claude/_docs/python/basefunctions/config.md existiert
When die Datei gelesen wird
Then beschreibt sie: Architektur (App-controlled), alle 4 öffentlichen Methoden mit vollständiger Signatur,
     deep-merge Verhalten, Self-Registration Pattern, Error-Handling, Thread-Safety
```

## AC-2: User Doc vollständig und korrekt
```gherkin
Given docs/basefunctions/config.md existiert
When die Datei gelesen wird
Then beschreibt sie: Wie lade ich Config als App, wie registriere ich Defaults als Package-Autor,
     wie lese ich Config-Parameter, vollständige API mit Parametern und Beispielen
     AND beschreibt NICHT die alte API (load_config_for_package, YAML, .get(), .has())
```

## AC-3: Docs entsprechen dem tatsächlichen Code aus Plan 01
```gherkin
Given 02-01-SUMMARY.md beschreibt was implementiert wurde
When die Dokumentation gegen den Code verglichen wird
Then alle dokumentierten Methoden existieren im Code
     AND keine deprecated Methoden sind dokumentiert
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: System-Dokumentation neu schreiben</name>
  <files>~/.claude/_docs/python/basefunctions/config.md</files>
  <action>
    Erstelle/überschreibe System-Dokumentation basierend auf system_documentation.md Template.

    Pflichtinhalte:
    - **Architektur-Konzept**: App-controlled loading, Self-Registration Pattern, Merge-Strategie
    - **Vollständige API** (alle 4 public Methoden):
      - `load_config_file(file_path: str) -> None` — App lädt explizit per Pfad, deep-merge
      - `register_package_defaults(package_name: str, config_path: str | Path) -> None` — Package registriert defaults, sofort geladen, silent wenn fehlt
      - `get_config_for_package(package: str | None = None) -> dict[str, Any]` — liest Sektion oder alles
      - `get_config_parameter(path: str, default_value: Any = None) -> Any` — slash-separated navigation
    - **_deep_merge Erklärung**: Warum deep-merge statt shallow (nested keys erhalten)
    - **Singleton Pattern**: Thread-safety via RLock, _singleton_instances
    - **Self-Registration Flow**: import Package → register_package_defaults → Defaults im Singleton
    - **Loading Reihenfolge**: Defaults first, App-Config last → App gewinnt
    - **Error Handling**: FileNotFoundError, JSONDecodeError, ValueError (kein .json), silent bei missing defaults
    - **Was ENTFERNT wurde und WARUM**: load_config_for_package hardcodet deployment path, nimmt App Kontrolle

    Format: Englisch, nach system_documentation.md Template, max 1000 Zeilen.
  </action>
  <verify>Datei existiert unter ~/.claude/_docs/python/basefunctions/config.md, ist lesbar, max 1000 Zeilen</verify>
  <done>AC-1 satisfied: System Doc vollständig und korrekt</done>
</task>

<task type="auto">
  <name>Task 2: User-Dokumentation neu schreiben</name>
  <files>docs/basefunctions/config.md</files>
  <action>
    Erstelle/überschreibe User-Dokumentation basierend auf user_documentation.md Template.

    Pflichtinhalte:
    - **Konzept erklären** (kurz): App lädt Config, Libs lesen nur. Defaults via Self-Registration.
    - **Quickstart für App-Entwickler**:
      ```python
      from basefunctions import ConfigHandler
      # Schritt 1: Defaults werden automatisch geladen (durch Package-Import)
      import tickerhub  # → registriert tickerhub-Defaults automatisch
      # Schritt 2: App-Config laden (überschreibt spezifische Werte)
      ConfigHandler().load_config_file("/path/to/app-config.json")
      # Schritt 3: Lesen
      value = ConfigHandler().get_config_parameter("tickerhub/database/host")
      ```
    - **Quickstart für Package-Autoren** (wie registriere ich Defaults in meinem Package):
      ```python
      # in mypackage/__init__.py
      from basefunctions import ConfigHandler
      from basefunctions.runtime import get_runtime_config_path
      ConfigHandler().register_package_defaults("mypackage", get_runtime_config_path("mypackage"))
      ```
    - **Config-File Format** (JSON, Package-Sections):
      ```json
      {
        "mypackage": {
          "database": {"host": "localhost", "port": 5432},
          "logging": {"level": "INFO"}
        }
      }
      ```
    - **Public API** (vollständig mit Parametern, Returns, Raises, Beispiel):
      - `load_config_file(file_path: str) -> None`
      - `register_package_defaults(package_name: str, config_path: str | Path) -> None`
      - `get_config_for_package(package: str | None = None) -> dict[str, Any]`
      - `get_config_parameter(path: str, default_value: Any = None) -> Any`
    - **Merge-Verhalten erklären** (Defaults + App-Config = ein Dict, App gewinnt bei Konflikten)

    NICHT dokumentieren: load_config_for_package, create_config_for_package, YAML, .get(), .has()

    Format: Englisch, nach user_documentation.md Template, max 300 Zeilen.
  </action>
  <verify>docs/basefunctions/config.md existiert, max 300 Zeilen, keine deprecated API erwähnt</verify>
  <done>AC-2, AC-3 satisfied: User Doc vollständig, korrekt, keine deprecated API</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- Kein Python-Code in diesem Plan anfassen
- `src/basefunctions/` — READ-ONLY für Dokumentationszwecke
- `tests/` — nicht anfassen
- Andere Subpackage-Docs — nicht anfassen

## SCOPE LIMITS
- Nur config Subpackage dokumentieren
- Kein secret_handler in diesem Plan (eigene Doku)

</boundaries>

<verification>
Before declaring plan complete:
- [ ] ~/.claude/_docs/python/basefunctions/config.md existiert und hat max 1000 Zeilen
- [ ] docs/basefunctions/config.md existiert und hat max 300 Zeilen
- [ ] Beide Docs beschreiben register_package_defaults korrekt
- [ ] Keine deprecated Methoden (load_config_for_package etc.) in Docs erwähnt
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Beide Docs erstellt und korrekt
- System Doc: vollständige technische API + Architektur-Konzept
- User Doc: klare Anleitung für App-Entwickler und Package-Autoren
- Keine veraltete API dokumentiert
</success_criteria>

<output>
After completion, create `.paul/phases/02-neural-530-basefunctions-refactor-confighandler/02-02-SUMMARY.md`
</output>
