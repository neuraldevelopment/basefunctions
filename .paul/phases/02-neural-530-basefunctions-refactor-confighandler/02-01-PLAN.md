---
phase: 02-neural-530-basefunctions-refactor-confighandler
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/basefunctions/config/config_handler.py
  - src/basefunctions/__init__.py
  - tests/config/test_config_handler.py
autonomous: true
---

<objective>
## Goal
ConfigHandler auf App-controlled config loading umstellen: Deprecated Methoden entfernen, `register_package_defaults()` hinzufügen, `load_config_file()` auf deep-merge upgraden.

## Purpose
Trennung von Concerns: Apps kontrollieren Config-Loading (explizite Pfade). Libs registrieren nur ihre Defaults beim Import. Kein hardcoded Deployment-Pfad mehr in der Lib.

## Output
- `config_handler.py`: 4 Methoden entfernt, 1 neue Methode, deep-merge intern
- `basefunctions/__init__.py`: `load_config_for_package` → `register_package_defaults`
- `test_config_handler.py`: Alte Tests entfernt, neue Tests für neues Verhalten
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@src/basefunctions/config/config_handler.py
@src/basefunctions/__init__.py
@tests/config/test_config_handler.py
</context>

<skills>
## Required Skills (from SPECIAL-FLOWS.md)

| Skill | Priority | When to Invoke | Loaded? |
|-------|----------|----------------|---------|
| /python:python_code_skill | required | Vor jeder Python-Code-Erstellung | ○ |

**BLOCKING:** Required skills MUST be loaded before APPLY proceeds.

## Skill Invocation Checklist
- [ ] /python:python_code_skill loaded
</skills>

<acceptance_criteria>

## AC-1: Deprecated Methoden entfernt
```gherkin
Given ConfigHandler ist importiert
When der Code auf load_config_for_package(), create_config_for_package(),
     create_config_from_template(), _create_full_package_structure() zugreift
Then AttributeError — Methoden existieren nicht mehr
```

## AC-2: register_package_defaults lädt sofort
```gherkin
Given ConfigHandler Singleton ist leer
When register_package_defaults("mypkg", "/path/to/config") aufgerufen wird
     und /path/to/config/config.json existiert mit {"mypkg": {"key": "val"}}
Then handler.config["mypkg"]["key"] == "val" (sofort geladen, kein weiterer Aufruf nötig)
```

## AC-3: register_package_defaults ignoriert fehlendes File
```gherkin
Given ConfigHandler Singleton ist leer
When register_package_defaults("mypkg", "/nonexistent/path") aufgerufen wird
Then kein Fehler, handler.config bleibt unverändert
```

## AC-4: load_config_file nutzt deep-merge
```gherkin
Given ConfigHandler enthält {"pkg": {"a": 1, "b": 2}} (z.B. aus defaults)
When load_config_file() mit {"pkg": {"b": 99}} aufgerufen wird
Then handler.config["pkg"]["a"] == 1 (default erhalten)
     AND handler.config["pkg"]["b"] == 99 (app-wert überschreibt)
```

## AC-5: basefunctions __init__.py nutzt register_package_defaults
```gherkin
Given basefunctions wird importiert
When initialize() ausgeführt wird
Then register_package_defaults("basefunctions", get_runtime_config_path("basefunctions")) wird aufgerufen
     AND load_config_for_package wird NICHT aufgerufen
```

## AC-6: Alle Tests grün, 0 skipped, 0 warnings
```gherkin
Given alle Code-Änderungen sind abgeschlossen
When pytest tests/ ausgeführt wird
Then alle Tests pass, keine Warnings, keine skipped Tests
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Deprecated Tests entfernen + Deprecated Methoden aus ConfigHandler entfernen</name>
  <files>
    tests/config/test_config_handler.py,
    src/basefunctions/config/config_handler.py
  </files>
  <action>
    TDD-Schritt: Erst Tests für zu entfernende Methoden löschen, dann Methoden selbst entfernen.

    **tests/config/test_config_handler.py:**
    - Entferne alle Tests für `create_config_from_template` (4 Tests: Zeilen ~508-669)
    - Entferne alle Tests für `load_config_for_package` (2 Tests: Zeilen ~677-758)
    - Update Fixture `mock_basefunctions`: entferne nicht mehr benötigte Mocks:
      `ensure_bootstrap_package_structure`, `create_full_package_structure`, `get_runtime_template_path`
      (bleiben: `create_root_structure`, `get_runtime_config_path`, `setup_logger`, `get_logger`)
    - NICHT entfernen: Tests für `load_config_file`, `get_config_for_package`, `get_config_parameter`, Singleton

    **src/basefunctions/config/config_handler.py:**
    - Entferne Methoden: `load_config_for_package()`, `create_config_for_package()`,
      `create_config_from_template()`, `_create_full_package_structure()`
    - Entferne jetzt ungenutzte Imports: `import os`, `import shutil`
      (load_config_file verwendet noch open() — kein os.path mehr nötig)
    - Header-Version auf v3.4 erhöhen
    - Header Description aktualisieren: "App-controlled config loading — libs register defaults, apps load config"

    Boundary: `load_config_file`, `get_config_parameter`, `get_config_for_package` NICHT anfassen.
  </action>
  <verify>pytest tests/config/test_config_handler.py -v → alle verbliebenen Tests pass</verify>
  <done>AC-1 satisfied: Deprecated Methoden weg, Tests aktualisiert, keine Regressions</done>
</task>

<task type="auto">
  <name>Task 2: register_package_defaults + deep-merge implementieren (TDD)</name>
  <files>
    tests/config/test_config_handler.py,
    src/basefunctions/config/config_handler.py,
    src/basefunctions/__init__.py
  </files>
  <action>
    TDD-Zyklus: Erst failing test, dann Implementierung. Pro Zyklus genau ein Test.

    **Zyklus 1 — register_package_defaults happy path:**
    1. Schreibe failing test: `test_register_package_defaults_loads_defaults_immediately`
       - tmp_path / "config.json" mit {"mypkg": {"key": "val"}}
       - assert handler.config["mypkg"]["key"] == "val" nach dem Call
    2. Implementiere `register_package_defaults(self, package_name: str, config_path: str | Path) -> None`:
       - `path = Path(config_path) / CONFIG_FILENAME`
       - Wenn nicht existiert: logger.debug + return (kein Fehler)
       - Wenn existiert: JSON laden, deep-merge in self.config (über `_deep_merge`)
       - Thread-safe via self._lock
    3. Test grün

    **Zyklus 2 — register_package_defaults missing file:**
    1. Failing test: `test_register_package_defaults_silently_ignores_missing_config_file`
       - nonexistent path → kein Fehler, config leer
    2. Bereits durch Implementierung gedeckt → grün

    **Zyklus 3 — load_config_file deep-merge:**
    1. Failing test: `test_load_config_file_deep_merges_nested_keys`
       - Preset handler.config = {"pkg": {"a": 1, "b": 2}}
       - load_config_file mit {"pkg": {"b": 99}}
       - assert handler.config["pkg"]["a"] == 1 (erhalten) AND ["b"] == 99 (überschrieben)
       - Test MUSS mit aktuellem code FEHLSCHLAGEN (shallow update ersetzt ganzen "pkg" key)
    2. Implementiere `_deep_merge(base: dict[str, Any], override: dict[str, Any]) -> dict[str, Any]`:
       - Rekursiver deep merge: für dict-values rekursiv mergen, sonst override gewinnt
       - Als Modul-level Funktion (nicht Methode, kein self), private: `_deep_merge`
    3. Update `load_config_file`: ersetze `self.config.update(config)` mit
       `self.config = _deep_merge(self.config, config)`
    4. Test grün

    **Zyklus 4 — register_package_defaults thread-safe:**
    1. Failing test: `test_register_package_defaults_thread_safe_concurrent_registration`
       - 5 Threads rufen gleichzeitig register_package_defaults mit verschiedenen packages
       - assert alle packages in handler.config
    2. Durch threading.RLock bereits abgedeckt → grün

    **basefunctions/__init__.py:**
    - Line 593: Ersetze
      `ConfigHandler().load_config_for_package("basefunctions")`
      mit
      `ConfigHandler().register_package_defaults("basefunctions", basefunctions.get_runtime_config_path("basefunctions"))`
    - Kein Import-Change nötig (ConfigHandler bereits importiert)

    **Wichtig:**
    - `load_config_file` Signatur bleibt `str` (kein Breaking Change für App-API)
    - `register_package_defaults` akzeptiert `str | Path` (flexibel für Callers)
    - Keine os.path in neuen Methoden — ausschließlich pathlib.Path
    - Alle Docstrings im numpy-Format mit Type Hints
  </action>
  <verify>
    pytest tests/config/test_config_handler.py -v --tb=short
    pytest tests/ --cov=basefunctions.config --cov-report=term-missing → ≥80% coverage
  </verify>
  <done>AC-2, AC-3, AC-4, AC-5, AC-6 satisfied</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE
- `src/basefunctions/config/secret_handler.py` — nicht in scope
- `src/basefunctions/config/__init__.py` — keine Änderungen (ConfigHandler bleibt exportiert)
- `load_config_file()` Signatur — bleibt `str`, nur intern deep-merge
- `get_config_parameter()` — nicht anfassen
- `get_config_for_package()` — nicht anfassen
- Alle anderen Subpackages (events, http, kpi, etc.) — TABU
- Alle Packages außerhalb basefunctions — TABU (tickerhub, signalengine etc.)

## SCOPE LIMITS
- Kein Cleanup von App-Skripten die load_config_for_package rufen → separates Issue
- Keine neuen Subpackages, keine neuen Files
- Keine Version in pyproject.toml anfassen

</boundaries>

<verification>
Before declaring plan complete:
- [ ] pytest tests/config/test_config_handler.py -v → alle Tests pass, 0 skipped, 0 warnings
- [ ] pytest tests/ → alle 2356+ Tests pass (keine Regression)
- [ ] Kein `import os` und kein `import shutil` in config_handler.py
- [ ] `load_config_for_package`, `create_config_for_package`, `create_config_from_template`, `_create_full_package_structure` existieren NICHT mehr in config_handler.py
- [ ] `register_package_defaults` in config_handler.py vorhanden
- [ ] `_deep_merge` in config_handler.py vorhanden
- [ ] basefunctions/__init__.py line ~593: register_package_defaults Call, kein load_config_for_package
- [ ] All acceptance criteria met
</verification>

<success_criteria>
- Alle Tasks abgeschlossen
- Alle Verification Checks grün
- Keine neuen Errors oder Warnings
- 4 deprecated Methoden entfernt, 1 neue Methode + 1 private Hilfsfunktion hinzugefügt
- Deep-merge aktiv in load_config_file
- basefunctions.__init__ nutzt register_package_defaults
</success_criteria>

<output>
After completion, create `.paul/phases/02-neural-530-basefunctions-refactor-confighandler/02-01-SUMMARY.md`
</output>
